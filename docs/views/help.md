# `:help` — in-app help

Alias: `?`. Every view with its aliases and one-line description, the global keys, the CLI commands,
and the custom-view schema — without leaving the terminal.

It's generated from the view registry, so it can't drift from what's actually installed, including
any custom views you've added.
